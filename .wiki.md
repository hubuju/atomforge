# Project Summary
AtomForge 是一个“对话驱动生成可运行网页应用”的工作台产品：用户在工作区用对话描述需求，系统把需求拆解为可执行的多文件前端项目，并在沙箱 iframe 中即时打包预览；静态检查与运行预警会把问题回传到工作台，支持“一键让 AI 修复”、保留对话/文件快照与版本历史以便回滚；用户可将工作区发布为免登录只读分享页，通过 share_slug 公开运行。

# Project Module Description
- 前端路由与产品入口
  - `/`：项目列表/管理中心（检索排序与模板相关入口）
  - `/auth`：注册/登录
  - `/w/:id`：项目工作台（对话生成/多文件编辑/iframe 沙箱预览/自检审计与修复/版本历史与回滚/发布分享）
  - `/s/:slug`：免登录只读分享页（按 slug 拉取已发布多文件并在 iframe 中运行预览）
  - `*`：NotFound
- 会话与后端数据访问（前端 SDK）
  - `src/lib/client.ts`：基于本地 token 解析会话；封装 `/api/v1/hub/*` 的账号/登录/me、workspaces、messages、versions 等请求；并补齐模板相关 API 类型与调用入口
- 模板体系（内置 + 个人）
  - `src/lib/templates.ts`：内置 starter templates 与匹配策略（keywords 命中/名称加权），输出候选与文件集合
  - `src/components/TemplatePicker.tsx`：模板选择器（内置分类浏览 + 个人模板列表 + 删除），用于创建流程与工作台“建议模板”确认
- AI 生成、解析与预览构建/运行探针
  - `src/lib/settings.ts`：模型/端点与生成行为设置（真实生效）；支持 atoms 代理与 compat(OpenAI-compatible SSE)；多智能体 pipeline 开关、角色模型覆盖、最大文件数、自动自检/自动修复上限等
  - `src/lib/agent.ts`：经典单次生成与预览运行链路（可复用流式传输能力供多智能体编排器调用）
  - `src/lib/orchestrator.ts`：多智能体编排器（Planner→Coder×N→Reviewer→Fixer）
  - `src/lib/roles.ts`：角色提示与结构化 Spec/Finding 定义；JSON 提取、单文件写出清洗、Reviewer 输出解析与修复策略
- 自检审计（静态+运行时）
  - `src/lib/audit.ts`：静态审计与运行时 iframe probe DOM/console/error 回报；把阻塞项转为修复提示词
  - `src/components/AuditPanel.tsx`、`src/components/Pipeline.tsx`：审计结果展示与生成流水展示
- 工作台与设置 UI
  - `src/pages/Workspace.tsx`：工作台主控（模板选择/建议与创建对话整合；统一进入静态审计+运行预览回传修复链路）
  - `src/components/SettingsDialog.tsx`：设置弹窗（适配模板选择、主题/审计等相关状态入口与交互）
- 主题（新增/扩展）
  - `src/lib/theme.ts`：light/dark/system 偏好持久化与 resolved 推导；导入时对 `<html>` 预涂色并设置 `color-scheme`；系统主题变更监听支持“跟随系统”
  - `src/components/ThemeToggle.tsx`：顶部主题切换入口（轻/深/跟随系统）
  - `App.tsx` / `AppShell.tsx` 及相关组件：主题一致性与语义颜色适配；`sonner` toast 使用 resolved 主题避免不一致

# Directory Tree
- `app/`
  - `backend/`
    - `routers/`：`hub.py` `/api/v1/hub/*` 主 API、`workspace_versions.py`、`share.py` 分享读取
    - `alembic/versions/`：模板相关迁移
    - `models/`：用户模板模型
    - `data_models/`：用户模板数据结构定义
    - `services/`：用户模板业务服务封装
  - `frontend/`
    - `src/`
      - `pages/`：`Index.tsx`、`Auth.tsx`、`Workspace.tsx`、`Share.tsx`
      - `components/`：`TemplatePicker.tsx`、`SettingsDialog.tsx`、`AuditPanel.tsx`、`Pipeline.tsx`、`RoleLanes.tsx`、`SpecPanel.tsx`、`ThemeToggle.tsx` 等
      - `lib/`：`client.ts`、`settings.ts`、`agent.ts`、`orchestrator.ts`、`roles.ts`、`audit.ts`、`templates.ts`、`bundler.ts`、`exporter.ts`、`theme.ts`
      - `assets/`、`uploads/`
    - `test/`（新增）
      - `*.test.ts`：核心逻辑单元测试（agent/audit/bundler/roles/settings/theme 等）
      - `README.md`：测试说明
    - `vitest.config.ts`（新增）

# File Description Inventory
- 新增
  - `app/frontend/test/README.md`：核心逻辑单元测试说明与覆盖范围
  - `app/frontend/test/vitest.config.ts`：Vitest 测试入口配置（与 app 的 vite 构建隔离）
  - `app/frontend/test/agent.test.ts`：验证生成流解析/写入合并/预览构建与 probe 消息解析等
  - `app/frontend/test/audit.test.ts`：验证静态与运行时审计规则、合并结果与修复提示词
  - `app/frontend/test/bundler.test.ts`：验证文件类型识别、入口 HTML 组装、预览内联与 CDN 引用保留
  - `app/frontend/test/roles.test.ts`：验证 Spec/Finding 清洗、角色消息拼装、解析与修复上下文构造
  - `app/frontend/test/settings.test.ts`：验证设置持久化、兼容/校验、角色覆盖与 URL 推导
  - `app/frontend/test/theme.test.ts`：验证主题预涂色、system 跟随、偏好读写与系统监听行为
- 主题相关新增/变更（源代码）
  - `app/frontend/src/lib/theme.ts`：主题偏好/resolved 推导与预渲染涂色
  - `app/frontend/src/components/ThemeToggle.tsx`：主题切换 UI
- 主题一致性变更（源代码）
  - `app/frontend/src/App.tsx`、`src/components/AppShell.tsx`、`AuditPanel/CodeViewer/Pipeline/RoleLanes/SpecPanel/SettingsDialog.tsx`、`src/index.css`、`src/pages/Auth.tsx`、`src/components/ui/sonner.tsx`：确保首屏无闪与语义颜色随主题一致

# Technology Stack
- 前端：React 18 + TypeScript + Vite + Tailwind CSS + shadcn/ui + react-router-dom
- AI：Atoms Cloud（代理流式）；兼容模式支持 OpenAI-compatible SSE 流解析
- 运行隔离：iframe sandbox + srcDoc；注入 storage shim 与 runtime probe，通过 postMessage 回传 console/error/DOM 运行报告
- 状态/组件：React hooks + sonner/toast
- 导出：JSZip（浏览器端 zip 打包）
- 后端：Python（hub 路由）+ ORM + Alembic 迁移
- 测试：Vitest + jsdom（新增，隔离 app 构建依赖）

# Usage
## 开发/构建
- 进入 `app/frontend`
- 安装依赖：`pnpm i`
- 运行检查与构建：`pnpm run lint`、`pnpm run build`

## 测试
- `pnpm run test`
- `pnpm run test:watch`

## 使用流程（产品层）
- 在首页或工作台进入“创建/开始”流程
- 如启用模板：
  - 可选内置模板或个人模板，选择后将模板文件直接作为可运行项目起点
  - 对于“简要需求命中内置模板”的场景，展示建议但需要用户确认后才会应用模板
- 在工作台输入需求：
  - 若启用多智能体：先 Planner 生成结构化 Spec，并在 confirmSpec 开启时暂停供用户确认/编辑；确认后进入 Coder→Reviewer→Fixer 编排写出多文件
  - 经典模式保留现有单次生成链路
- 自动/手动触发静态审计与 iframe 运行自检；发现问题可“一键让 AI 修复”
- 查看版本历史、支持回滚；可发布为免登录分享页或导出 zip/standalone/单文件
