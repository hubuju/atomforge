{
    "title": "code_versions",
    "type": "object",
    "description": "Code snapshots saved on every generation round for history and rollback",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Autoincrement primary key"
        },
        "user_id": {
            "type": "string",
            "description": "Owner user id"
        },
        "project_id": {
            "type": "integer",
            "description": "Related project id"
        },
        "version_no": {
            "type": "integer",
            "description": "Incremental version number inside the project"
        },
        "code": {
            "type": "string",
            "description": "Snapshot of the single-file HTML source code"
        },
        "summary": {
            "type": "string",
            "description": "Short summary of what changed in this version"
        }
    },
    "required": [
        "project_id",
        "version_no",
        "code",
        "user_id"
    ]
}